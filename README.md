# videocourses

Simple React + Vite starter with the template demo code removed.

## Structure

- `src/App.jsx` main app component
- `src/main.jsx` React entry point
- `src/index.css` global styles

## Scripts

- `npm run dev`
- `npm run build`
- `npm run lint`
- `npm run preview`
